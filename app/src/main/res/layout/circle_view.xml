<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/circleColor"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/ic_empty"
        app:civ_circle_background_color="@color/backgroundColor"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/circleText"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:gravity="center"
        android:textColor="@color/textColor"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/circleColor"
        app:layout_constraintEnd_toEndOf="@+id/circleColor"
        app:layout_constraintStart_toStartOf="@+id/circleColor"
        app:layout_constraintTop_toTopOf="@+id/circleColor" />
</androidx.constraintlayout.widget.ConstraintLayout>